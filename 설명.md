# Todo App 프로젝트 설명

## 프로젝트 개요
React Native와 Expo를 사용한 할 일 관리(Todo) 애플리케이션입니다. 웹과 모바일 환경 모두에서 사용 가능한 크로스 플랫폼 앱입니다.

## 주요 기능
1. **할 일 관리**
   - 할 일 추가/수정/삭제
   - 제목, 설명, 우선순위, 마감일 설정
   - 완료 상태 토글

2. **필터링 및 정렬**
   - 전체/진행 중/완료된 할 일 필터링
   - 생성일/우선순위/마감일 기준 정렬

3. **알림 기능**
   - 마감일 기준 알림 설정
   - 완료 시 알림 자동 취소

## 기술 스택
- React Native
- Expo
- TypeScript
- AsyncStorage (로컬 데이터 저장)
- React Navigation (화면 전환)
- Expo Notifications (알림 기능)

## 프로젝트 구조
```
todo-app/
├── src/
│   ├── components/         # 재사용 가능한 컴포넌트
│   │   ├── TodoItem.tsx   # 할 일 항목 컴포넌트
│   │   └── FilterBar.tsx  # 필터/정렬 컴포넌트
│   ├── context/
│   │   └── TodoContext.tsx # 상태 관리
│   ├── screens/
│   │   ├── HomeScreen.tsx  # 메인 화면
│   │   └── AddEditTodoScreen.tsx # 할 일 추가/수정 화면
│   ├── utils/
│   │   └── dateUtils.ts   # 날짜 관련 유틸리티
│   └── types.ts           # TypeScript 타입 정의
└── assets/                # 이미지 등 정적 자원
```

## 주요 컴포넌트 설명

### TodoItem.tsx
- 개별 할 일 항목을 표시
- 스와이프로 수정/삭제 기능 제공
- 체크박스로 완료 상태 토글

### FilterBar.tsx
- 할 일 목록 필터링 및 정렬 기능
- 전체/진행 중/완료 필터
- 생성일/우선순위/마감일 정렬

### TodoContext.tsx
- 앱의 상태 관리
- AsyncStorage를 통한 데이터 영속성
- 알림 예약 및 취소 관리

### AddEditTodoScreen.tsx
- 할 일 추가/수정 폼
- 우선순위 선택
- 마감일 설정 (웹/모바일 환경 대응)

## 실행 방법
1. 의존성 설치:
```bash
npm install
```

2. 앱 실행:
```bash
npx expo start
```

3. 실행 옵션:
- 웹 버전: `w` 키 입력
- Android: `a` 키 입력
- iOS: `i` 키 입력
- QR 코드 스캔: Expo Go 앱으로 스캔

## 참고사항
- 웹 환경에서는 일부 네이티브 기능이 제한될 수 있습니다.
- 알림 기능은 실제 디바이스에서만 완전히 작동합니다.
